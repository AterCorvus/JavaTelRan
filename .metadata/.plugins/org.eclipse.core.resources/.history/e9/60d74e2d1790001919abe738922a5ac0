package predicates;

import java.util.function.Predicate;

public class MultipleOfRange implements Predicate<Integer>{

	private int from;
	private int until;
	
	
	
	public MultipleOfRange(int from, int until) {
		super();
		this.from = from;
		this.until = until;
	}



	@Override
	public boolean test(Integer t) {
		return t > from && t < until; 
	}

}
